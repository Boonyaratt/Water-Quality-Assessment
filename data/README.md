# Data Directory

This directory is intentionally kept mostly empty in Git.

Expected local structure:

```text
data/raw/
data/interim/
data/processed/
data/external/
```

Raw and processed research data should be stored locally, on external storage, or in a controlled cloud drive. Do not commit raw datasets, large CSV files, image patches, GeoTIFFs, or restricted station data to GitHub.

