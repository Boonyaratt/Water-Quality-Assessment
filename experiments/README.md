# Experiments Directory

This directory is reserved for local model outputs and experiment artifacts.

Do not commit AutoGluon model folders, trained models, predictions, logs, or large intermediate outputs unless they are intentionally curated as small summary tables under `reports/tables/`.

